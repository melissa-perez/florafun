<div class='container border border-light rounded border-2 mb-2'>
  <form
    method='GET'
    id='{{toLowerCase tableId}}-html-search'
    action='/{{toLowerCase tableId}}'
    class='mb-3'
  >
    <legend>Search for a {{removeLastLetter tableId}}</legend>
    <fieldset>
      <p>Search by name using the field below.</p>
      <label clas='form-label' for='{{toLowerCase tableId}}_name'>
        {{removeLastLetter tableId}}
        name:
      </label>
      <input
        class='form-control'
        type='text'
        name='{{toLowerCase tableId}}_name'
      />
      <div class='form-text'>
        The search will look for general results that match the given term.
      </div><br />
      <button
        class='btn btn-primary my-2 my-sm-0 btn-sm'
        type='submit'
      >Search</button>
      <a
        class='btn btn-warning my-2 my-sm-0 btn-sm'
        href='/{{toLowerCase tableId}}'
        role='button'
      >Reset search</a>
    </fieldset>
  </form>
</div>